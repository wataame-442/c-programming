#include <stdio.h>

float dentaku(float a,float b,float c);

int main(void){
    int mode;
    float a,b,n;
    printf("モードを選択してください【1：足し算　2：引き算　3：掛け算】\n");
    scanf("%d",&mode);
    printf("一つ目の値を入力してください\n");
    scanf("%f",&a);
    printf("二つ目の値を入力してください\n");
    scanf("%f",&b);
    printf ("答えは.%fです",dentaku);
    if(mode==1){
        printf("足し算を選択しました");
    n=tasu(a,b);
    }
    else if(c==2){
        printf("足し算を選択しました");
    n=hiku(a,b);
    }
    else if(c==3){
        printf("足し算を選択しました");
    n=kakeru(a,b);
    }
    float dentaku(float d,float c){

    }
}
